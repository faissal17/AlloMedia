const { inMemory: inMemoryDb } = require("../../database");

module.exports = {
  add: async (cuisine) => {
    return cuisine;
  },
  update: async (cuisine) => {
    return cuisine;
  },
  delete: async (cuisine) => {
    return cuisine;
  },
  getById: async (id) => {
    return id;
  },
};
